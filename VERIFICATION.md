## 最新验证：移除展示歌曲（2026-09-15）

- 《为了你唱下去》从运行缓存移至 `data/raw/song-c1bf1af0cdee87fc-archived.json`，原始材料保留。
- 重启本地服务后，`GET /api/catalog` 仅返回《反乌托邦》《I LOVE U》《白石溪》，三首歌曲接口均返回 200。
- 已移除歌曲接口与归档文件的 HTTP 路径均返回 404。
- `npm run validate` 通过。本次未调用知乎搜索、未消耗检索额度，未执行部署。

# 最新验证：搜索重置与表格转置（2026-09-14）

实际本地服务与浏览器验证，未新增知乎检索或修改真实材料：

- 搜索《为了你唱下去》并提交后刷新，搜索框为空；首页仍显示全部四首已整理歌曲，包含该曲。
- 选择两词进入对比，再返回歌墙仍保留两词；返回搜索页后，再进入同一首歌，选词为零且比较按钮不可用。
- 两词比较为两行关键词、四列相关来源；五词比较为五行关键词、六列相关来源。另有一个左侧关键词标识列。对比页刷新仍保持本次五词。
- 核对每行关键词及每格 sourceId / keywordId 关系，转置没有补齐无依据的格子。s2:craft 展开对应“作曲简略，编曲单薄”；作者、标题、原句和来源继续对应。
- 聚焦操作作用于关键词行，支持恢复全部。行内展开 colspan 对应实际文章数加一，五词六篇内容时为 7。
- 临时同源容器中验证 320px、390px：表格独立横向滚动，页面宽度不溢出；横向找到文章后，展开 s1:visibility 显示正确上下文，长证据没有横向溢出。临时测试文件已移除。
- `npm run validate` 与已调整测试脚本的语法检查通过。既有浏览器脚本已更新方向和重置断言，本轮没有重新运行独立 headless 套件，不将历史结果计为本轮通过。

---

# 前轮验证：词墙侧栏与行内对照阅读（2026-09-14）

本轮通过浏览器实际操作验证新结构，真实数据与检索服务未变动。

- 桌面阅读栏为非模态，仍能直接点击墙上的其他词切换；查看不自动选择，选满五词后第六词在侧栏提示上限。侧栏长内容可独立滚动。
- 所有 16 个词的实际动画为 `word-fade`，计算 transform 均为 none，延迟从 0 到 330ms。淡入时词语位置不移动；切换词语与选词保留原有墙面 DOM。
- 两词／五词对比、列头聚焦、状态提示、恢复全部、原句行内展开、关闭、返回保留选择均正常。s2:craft 对应“作曲简略，编曲单薄”；s3:craft 保留证据不足且没有编造引文。对比证据不再使用遮罩弹窗。
- 以实际 HTML/CSS/JS 的临时同源容器验证 320px、390px 与 931px。320／390px 的歌墙无页面横向溢出，手机为底部抽屉；931px 使用侧栏。320px 五词表宽 1012px，来源列 112px、每个关键词列 180px；第五列证据展开宽 292px，无横向溢出，能够阅读正确语境。临时容器在验证后移除。
- 行内原文入口保留 API 返回的完整来源 URL（含来源参数）；页面错误日志为空。`npm run validate` 通过。
- `tests/browser.mjs`、`comparison-loading.mjs`、`live-presets.mjs` 与 `loading.mjs` 已将受影响的定位及淡入断言迁移到新结构；本轮未重新执行这些独立 headless 测试，其旧结果属于历史。本轮验证来自以上实际浏览器操作。

---

# 前轮验证：音乐展厅 UI 重构（2026-09-14）

使用当前本地服务与独立测试标签页，通过浏览器自动化实际操作。未修改真实数据，未触发新的知乎检索或消耗业务 API 额度。测试时目录包含三首预设及用户已缓存的《为了你唱下去》，新缓存原样保留。

- 搜索《反乌托邦》进入 16 词歌墙；另外核对 I LOVE U 的 12 词与白石溪的 14 词入口。清空输入并提交显示“请输入歌名或创作者”。
- 查看关键词不会选中；单词时比较按钮不可用；选两词可进入三列表；第五个词可加入，第六个词被拦截并在弹窗内提示；移除后可重新加入；返回和刷新保留选择。
- 1280px 桌面词墙按钮无重叠，字面频次越高字号越大，同频同字号。五词表格共六列；作者列与表头的实际计算样式均为 sticky，表格内容宽 1465px、可视宽 1206px，独立滚动。
- 分别查看 s2:craft 的“作曲简略，编曲单薄”及 s3:craft 的证据不足状态，后者没有编造引文。空格保留“不代表原文没有讨论”。
- 原文按钮实际打开 `https://www.zhihu.com/question/15570446264/answer/1905309411175532240?utm_medium=openapi_platform&utm_source=cf621feb3f2d`，新标签页标题为对应《反乌托邦》问题。
- 浏览器视口覆盖能力未改变实际页面尺寸，改用临时同源测试容器加载本轮实际 HTML/CSS/JS。320px、390px 下的真实渲染文档宽度与 scrollWidth 相同，关键词无重叠；五词表格独立滚动，第五列可展开正确语境；390px 下证据对话框宽 370px。未改动生产 CSP，临时容器在验证后删除。
- 视觉检查覆盖桌面首页、连续词墙、详情弹窗、五词表格及窄屏词墙和证据面板。首次加载声波可见，实际表格的动画为 `table-reveal`；新词错峰及回弹样式、减少动态效果规则通过代码检查。650ms 的对比骨架未被本轮较慢的浏览器采样稳定捕获，因此不把旧版动画测试算成本轮实测；未重复执行实时 SSE 的历史测试。
- `npm run validate` 通过语法与真实数据校验：6 个原始来源、16 个关键词、30 条分析。测试标签页错误日志为空。所有 UI 变更无需安装、构建或服务器重启。

---

# 以下为重构前验证：额度恢复、真实新歌与丰富词墙

对比表加载及多角度说明：`tests/comparison-loading.mjs` 的 10 项检查通过，结果在 `tests/comparison-loading-results.json`。覆盖进入／刷新动画、加载中返回取消、两词及手机五词布局、证据展开、减少动态效果和无脚本错误。实际核对 0wzyt1 的回答已有 7 个关键词关系，表格中可展开查看；空格提示改为“当前未整理到对应证据”，不把未提取的关系当成内容未涉及。

新增统一加载动画验证：`tests/loading.mjs` 的 12 项检查通过，结果见 `tests/loading-results.json`。实际验证首次打开、三首预设加载、内存缓存取消；用隔离测试场景检查慢缓存切换、缓存失败重试、非预设 SSE 加载。桌面与手机的声波、流动条、进入歌墙的过渡及减少动态效果偏好均正常，无未捕获浏览器错误。缓存加载没有伪造网络检索步骤。取消与新搜索会使旧请求及过渡失效。

2026-09-14 18:48（Asia/Shanghai）完成三首歌实际服务验证，记录在 `tests/live-presets-results.json`，没有模拟新歌来源。此前额度耗尽已解除。

- 真实 CLI 搜索取得两首新歌材料；白石溪在线分析成功，I LOVE U 未通过自动分析校验后正常降级。最终三首均在准备阶段逐条复核，补充了不同阅读方法。
- 反乌托邦：6 位作者／16 词；I LOVE U：4 位作者／12 词；白石溪：4 位作者／14 词。
- 三首逐一验证搜索进入、五词选择上限、比较、展开证据、准确原文 href、返回及移除、各歌独立恢复选择。所有公开引文都可回溯研究文本。
- 三首在 1440px、390px、320px 均无关键词重叠或页面溢出，字号随实际次数变化，相同次数相同字号。页面无未捕获错误。
- `npm test` 的 7 个服务测试通过，包括提额后缓存过期恢复；`npm run validate` 通过 6 个原始来源、16 词、30 条分析校验。
- 18:49 再次完成 `tests/browser.mjs` 的 35 项主路径回归；额度错误分支现在使用明确测试场景。`tests/search-ui.mjs` 的 10 项进度、取消、降级与隔离测试也通过。
- 三首最新截图位于 `tests/screenshots/`，文件名包含歌曲 ID；已检查桌面和手机视觉。新歌只核对原文准确 href，不声称已取得外部全文。

---

以下为此前额度耗尽阶段的历史验证，数字和限制对应当时状态，以本文开头为准。

# 实际运行与交互验证 · v0.2

验证日期：2026-09-14。真实浏览器流程记录于 17:59，新增检索交互记录于 18:00（Asia/Shanghai）；最后重启服务并复核接口。地址 `http://127.0.0.1:4173`。独立 headless Chrome 153.0.8010.36，没有使用用户个人资料或登录态。

## 实际服务与既有真实数据

- `npm run validate` 通过：5 个来源、8 个关键词、20 条分析，原句及词频可回溯；18 条充分支持局部概括、2 条证据不足，其余格子不补齐。原始长文本不在公开展示 JSON 中。
- 官方 CLI 实际额度查询：知乎搜索 10/10 已用完，剩余 0。新歌请求显示额度错误，没有虚构搜索成功或伪装成无结果。《I LOVE U》《白石溪》只有预设元数据，尚无真实讨论入库。
- 重启后的真实 HTTP 核验：首页 200、三首目录状态正确、已有歌数据可读、新歌 SSE 返回 QUOTA_EXHAUSTED、研究文件请求 404、缺少页面请求标识的搜索被拒绝。
- `tests/browser.mjs` 的 35 项断言通过，浏览器未捕获错误 0。完整记录在 `tests/results.json`。

| 范围 | 已验证行为 |
|---|---|
| 搜索 | 首屏搜索框、三首预设的可阅读／待整理状态、空输入、别名与标点、特殊字符安全显示、实际额度错误 |
| 彩色墙 | 八色关键词组成同一面墙，固定排布，无重叠；字号随实际匹配次数增大，同次数同字号，详情展示计数词表 |
| 查看与选择 | 查看不误选；0–1 词不能比较；2–5 词可比较；第六词被拒绝；可移除后补入 |
| 对比与证据 | 关键词列、具体文章／回答行，五列可滚动且行列表识固定；证据弹窗正确，缺格和证据不足不编造内容 |
| 语境 | 保留“你个怪物”过去批评与后来反思的区别；原句、AI 概括、上下文分别展示 |
| 原文 | 实际点击打开新页面，导航 URL 与 API 原始 URL 完全一致；未取得可读知乎正文响应，不能声称正文加载成功 |
| 状态与可访问性 | 返回与刷新保留选择，Esc 关闭恢复焦点，无有效选择的比较路由回退，数据错误后可重试 |
| 窄屏与长文本 | 390px 歌墙、五列对比、长证据可读，320px 搜索可用，页面无横向溢出 |

## 检索成功路径的隔离测试

由于搜索额度为零，以下成功路径使用历史真实《反乌托邦》材料作为测试输入，**不是新歌曲线上检索成功的证明**。没有将测试数据写入生产歌曲库。

`npm test`：6 个 Node 测试通过。覆盖已有缓存不消耗 API；任意查询经筛选和原句降级、持久保存及重启复用；AI 精确引用校验并拒绝伪造引文；额度耗尽时停止业务请求；无关、匿名、重复和不合法来源过滤；取消释放任务状态、不缓存半成品。文件：`tests/search-service.test.mjs`。

`tests/search-ui.mjs`：10 项断言通过，浏览器错误 0。其中前两项使用真实服务验证两首新预设的额度响应；其余使用 HTTP/SSE 测试桩验证四阶段动画、取消和迟到响应隔离、成功响应进入独立词墙、无 AI 时的原句标记、按歌曲隔离并恢复选择、390px 动态布局、减少动态效果偏好。记录：`tests/search-ui-results.json`。

## 视觉与限制

已查看新版搜索首屏和等待进度截图，并保留桌面歌墙、五列比较与窄屏证据截图。`tests/screenshots/progress-test-fixture.png` 和 `runtime-mobile-test-fixture.png` 来自明确标记的测试桩；其他截图来自已有真实歌墙和目录。

尚未验证额度恢复后的新歌线上检索及直答分析成功，无法报告整个音乐曲库已取得。自动分析的引用校验不等于语义准确性的人工保证。知乎正文外链可读性受外部网络策略影响。WebMCP 仅做功能检测，该浏览器无相关接口，未验证真实调用；普通页面不依赖它。未部署、发布或提交作品。
