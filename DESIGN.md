# 歌墙 · 音乐展厅视觉重构

## 当前更新：搜索重置与表格转置

- 搜索文字不再写入或从 sessionStorage 恢复。首页刷新和返回搜索页均为空搜索框，并显示全部已整理歌曲。
- 返回搜索页会清空各歌的临时选词，同时取消仍在等待的请求；歌曲目录和真实数据缓存不受影响。从对比返回歌墙、刷新对比页仍可继续本次选择。
- 表格改为文章／回答按列、关键词按行。顶部固定文章、作者与原文入口；左侧固定彩色关键词。横向比较不同来源对同一关键词的表达，纵向阅读一篇内容的多个角度。
- 聚焦操作随之改为聚焦关键词行。行内证据保持具体来源标题、作者及原文链接，跨列数量由实际来源列数决定；加载骨架也按新方向生成。

以下为此前设计记录；当前行为以上述更新为准。

## 后续更新：连续词墙与来源阅读带

按确认方案重构后两页：900px 及以上使用固定侧边阅读栏，可直接点击其他词切换内容；更窄的窗口使用底部模态抽屉。桌面预留阅读角落，打开详情不会重新排列词墙。选词栏继续保留 2–5 词规则。

全部词语连同次数采用纯透明度淡入，单词持续 470ms、间隔 22ms，最大延迟 330ms，总时长不超过 800ms。只在进入歌墙、创建这面墙时播放；切词与选词不重建词墙，不再次播放。词频仍决定字号。

对比页改为彩色列头与来源阅读带；列头颜色与词墙一致，可聚焦一列或恢复全部。每篇具体内容保持一行，证据在该行下方展开，包含 AI 概括、原句、语境、证据范围及来源。收起或按 Esc 恢复触发按钮焦点。手机作者列缩为 112px，剩余视口宽度用于一列观点，避免最后一列被固定来源列遮住。

以下记录保留第一轮重构的参考过程；后两页的实际行为以上述更新为准。

2026-09-14。保留歌曲搜索、真实材料、词频字号、关键词详情、2–5 词对比和原文入口。本次只重构呈现与动效，不修改分析内容或扩大产品功能。

## 参考与取舍

- [知阶 Knowledge Steps](https://ksteps.yinbo.online/)：参考清晰的阶段引导、留白、层叠卡片。通过其公开 HTML/CSS/JS 阅读结构与动画定义；未完整实测该站交互。
- [旅行看山：答主之路](https://zukotzlgbxbp.sealoshzh.site/)：参考柔和配色、具有触感的收集反馈和轻微偏移阴影。通过公开样式与结构研究，不复制其素材或功能。
- [Anime.js](https://animejs.com/)：参考官网展示的 stagger、时间编排与弹性节奏。只应用于一次性的词语入场、选择反馈及等待衔接。用原生 CSS 实现，不新增依赖或 CDN 请求。

## 视觉与动效

- 首页为音乐展厅入口：搜索居前，抽象唱片与纸质展签构成视觉意象，歌曲以三列展品呈现；手机改为紧凑图文卡片。
- 蓝、粉、绿唱片图形由 CSS 绘制，是装饰图案，不是歌曲封面或播放控件。
- 页头标记找歌、歌墙、对照阅读三个阶段。阶段标识是当前位置说明，避免不可用的导航按钮。
- 关键词仍铺在同一连续墙面，使用真实词频决定字号；点击详情与加入对比保持分离。底部选词栏始终可见。
- 对比墙使用稳定的作者列与表头；AI 概括、原句、证据范围分层显示。五词时表格独立滚动。
- 搜索首页及歌曲卡片淡入约 450–800ms；词语按 22ms 间隔依次出现，延迟最多 330ms。只移动词语内部标签，点击区域固定。
- 新加入的选词做约 380ms 的轻回弹，既有选词不重复弹跳。原文正文不持续运动。
- 已有歌曲与实时检索共用声波语言，实时阶段仍由服务端进度驱动；对比加载后表格淡入约 450ms。
- `prefers-reduced-motion: reduce` 统一停止动画和过渡，既有 JS 同时跳过展示延时。

## 实现

`dist/styles.css` 重写为 base、layout、exhibition、reading、motion、responsive 六个 CSS 层，替代旧版不断叠加的样式覆盖。`dist/app.js` 增加统一抽象唱片组件、阶段状态和仅针对新增选词的动画标记。运行方式不变：`npm start`，无需构建。

本轮实际浏览器验证与限制见 `VERIFICATION.md` 开头。旧截图及旧自动化记录保留为历史，不冒充本次新界面的验证结果。
