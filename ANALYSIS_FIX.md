# 弦外：新歌曲分析修复（2026-09-15）

## 本次处理

- Render 已有 `ZHIHU_ACCESS_SECRET` 时，分析直接使用知乎官方直答流式 API；知乎内容搜索仍由已安装的知乎 CLI 完成。没有新增模型供应商、费用或凭证。
- 分析请求允许等待 100 秒，覆盖连接和响应正文；区分网络失败、超时、认证、限流、空响应、截断及证据校验失败。
- 分析失败不再用普通分词凑成歌墙。保留候选来源，显示“重新分析已有材料”，不重复消耗搜索调用。
- 旧版自动截取缓存显示为待分析，成功重试后更新为有证据的关键词墙；失败后至少等待 30 秒再重试。
- 提示词排除纯歌词、简单推荐和“你是”等代词；至少两个角度通过原句校验，才开放对照阅读。

## 你现在怎么更新

1. 解压 `xianwai-analysis-fix-20260915.zip`，打开解压后的文件夹。
2. 打开 GitHub 仓库的根目录，选择 **Add file → Upload files**。把修复包内的 `dist`、`scripts`、`prompts`、`tests` 文件夹，以及 `package.json`、本说明文件一起拖入。不要把最外层的 `xianwai-analysis-fix-20260915` 文件夹拖进去，也不要直接上传 ZIP。
3. 检查上传列表中路径是 `scripts/zhihu-provider.mjs`、`dist/app.js` 这样的路径，然后 **Commit changes**。修复包没有 `scripts/serve.mjs`，不会覆盖你已改好的云端监听设置。
4. 到 Render 等此次提交部署成功。若未自动部署，点 **Manual Deploy → Deploy latest commit**。原来的构建命令、启动命令、Free 套餐和环境变量均不用改。
5. 刷新网站，搜索《勾指起誓》。如果仍有旧缓存，打开其来源页并点 **重新分析已有材料**；如果重部署清除了临时缓存，重新搜索会正常取得材料再分析。

无需在本地输入密钥，也无需重建 Token。

## 更新后的验收

- 搜索后，成功分析应出现有意义的关键词和带原句的概括，选择两词可以对照阅读。
- 若知乎仍未返回有效分析，页面保留原文入口并显示实际错误。展开“查看本次状态”即可看到错误码；Render 日志也会记录 `xianwai-analysis-failed`、阶段和耗时，不记录密钥或请求正文。
- `TIMEOUT` / `NETWORK_ERROR` / `UPSTREAM_ERROR` 表示此次接口请求没成功；`INVALID_JSON` 是返回格式不合要求；`INSUFFICIENT_EVIDENCE` 是概括没有通过足够的逐字原句校验。它们不再都被误报成未配置认证。

## 已验证与仍需确认

已通过 `npm test` 全部 12 项测试、`npm run validate` 和 `git diff --check`。
浏览器已走通：旧失败缓存 → 来源页 → 刷新仍保留该来源 → 重试 → 有证据的关键词墙 → 查看详情 → 选择两个词 → 对照阅读 → 展开原句及语境。对照仍为文章列、关键词行，原始知乎链接保留。

上述恢复测试使用项目已有真实资料作为固定返回样本，未向演示库加入虚构内容，也未用测试结果冒充新的实时分析。官方流式接口的分块、超时、错误处理已做模拟响应测试，但新的云端直连路径尚未在 Render 实际运行。因此，此包修好了错误处理、失败缓存及重试机制，并提供了更直接的知乎调用路径；不能据此保证知乎上游服务已恢复。更新后的实际查询是最后一步验证。

此前在同一时段，本地和云端分析都失败，本地搜索仍成功。这说明不能只归因于部署，也不否认几小时前本地曾成功。
